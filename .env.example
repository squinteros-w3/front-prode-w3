# URL del backend NestJS para llamadas server-side (BFF)
BACKEND_API_URL="http://localhost:3000/api"
# URL publica del backend para el redirect de login con Google (la usa el navegador)
PUBLIC_BACKEND_API_URL="http://localhost:3000/api"
